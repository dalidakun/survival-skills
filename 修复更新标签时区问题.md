# 修复更新标签时区问题

## 问题
新文章没有显示"更新"标签。

## 原因
时区不匹配：
- `updatedAt` 是 ISO 格式（UTC 时区）
- 判断"今天"使用的是服务器本地时区
- 如果时区不同，日期可能不匹配

## 解决方案
已修复为使用 UTC 时区进行比较：
- 获取今天的 UTC 日期
- 将 `updatedAtMs` 转换为 UTC 日期
- 比较 UTC 日期字符串

## 修改内容
- `src/app/page.tsx`：使用 UTC 时区进行日期比较

## 测试
修复后，今天创建的文章应该会显示绿色的"更新"标签。

